// ─────────────────────────────────────────────────────────────
// js/ai-config.example.js — แบบฟอร์มไฟล์คีย์ AI
//
// วิธีใช้: คัดลอกไฟล์นี้เป็น js/ai-config.js แล้วแทนค่า apiKey ด้วยคีย์ของหลักสูตร
//          (ผู้สอนส่งให้ใน Google Classroom · ห้ามใช้คีย์ส่วนตัว)
//
// js/ai-config.js อยู่ใน .gitignore แล้ว ห้ามเอาออก
//    ไฟล์นี้ (ตัว .example) ขึ้น GitHub ได้ เพราะไม่มีคีย์จริง
// ─────────────────────────────────────────────────────────────

export const aiConfig = {
  apiKey: "ใส่-คีย์-OpenRouter-ที่ขึ้นต้นด้วย-sk-or-",
  model: "google/gemini-2.5-flash-lite"
};
