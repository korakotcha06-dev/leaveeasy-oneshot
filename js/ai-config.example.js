// ─────────────────────────────────────────────────────────────
// js/ai-config.example.js — เทมเพลตไฟล์ตั้งค่า AI
//
// วิธีใช้: คัดลอกไฟล์นี้เป็นชื่อ js/ai-config.js แล้วใส่ API key ของตัวเองแทนข้อความตัวอย่าง
// js/ai-config.js ถูกใส่ไว้ใน .gitignore แล้ว (ห้าม commit key จริงขึ้น git เด็ดขาด)
//
// ผู้ให้บริการที่โปรเจกต์นี้ใช้คือ OpenRouter (https://openrouter.ai)
// สมัครบัญชีแล้วสร้าง API key ได้ที่ https://openrouter.ai/keys
// เลือกโมเดลราคาถูกก็เพียงพอ เพราะงานนี้แค่ให้เลือกชื่อประเภทจากรายการสั้น ๆ
//
// ข้อจำกัดที่รู้อยู่แล้วและตั้งใจไม่แก้ในเดือนนี้ (บันทึกไว้ใน BACKLOG.md ของโปรเจกต์):
//   key ก้อนนี้ถูกส่งไปกับโค้ดฝั่งเบราว์เซอร์ตรง ๆ ใครเปิด devtools ก็เห็นได้
//   ระบบจริงต้องมีเซิร์ฟเวอร์กลางเป็นคนถือ key แทนไม่ให้หลุดถึงเบราว์เซอร์
//   แต่ scope ของงานนี้ไม่มีเซิร์ฟเวอร์ของเราเอง (กติกาข้อ 0 ห้าม Cloud Functions)

export const aiConfig = {
  apiKey: "ใส่ OpenRouter API key ของคุณตรงนี้",
  model: "openai/gpt-4o-mini"
};
