# CLAUDE.md — คู่มือประจำโครงงาน LeaveEasy

> ไฟล์นี้คือคู่มือที่ Claude Code อ่านทุกครั้งก่อนเริ่มทำงานในโครงงานนี้
> เขียนไว้ครั้งเดียว จะได้ไม่ต้องพิมพ์บริบทซ้ำทุกครั้งที่คุยกับ AI

---

## 1. โครงงานนี้คืออะไร

**LeaveEasy คือระบบขอลาออนไลน์ของที่ทำงานขนาด 20–100 คน**
พนักงานยื่นใบลาผ่านหน้าเว็บแทนการเขียนใบกระดาษ หัวหน้าพิจารณาอนุมัติหรือไม่อนุมัติ
แล้วผลถูกบันทึกไว้ให้ทุกฝ่ายเปิดดูย้อนหลังได้ที่เดียว

**เส้นทางหลักของระบบ:** พนักงานยื่นใบลา → หัวหน้าพิจารณา → อนุมัติหรือไม่อนุมัติ → บันทึกผล

**ผู้ใช้ของระบบ**

| บทบาท | ค่าในช่อง `role` | ทำอะไรได้ |
|---|---|---|
| ผู้ขอลา | `employee` | ยื่นใบลา · ดูใบลาของตัวเอง · ลบใบของตัวเองที่ยังเป็น `รอพิจารณา` |
| ผู้อนุมัติ | `manager` | ดูใบลาทุกใบ · เปลี่ยนสถานะเป็นอนุมัติหรือไม่อนุมัติ · เขียนความเห็น |
| ฝ่ายบุคคล | `hr` | ทำได้ทุกอย่างที่ผู้อนุมัติทำได้ · เพิ่ม แก้ ลบ ประเภทการลา |

---

## 2. เครื่องมือที่โครงงานนี้ใช้

**ห้ามเปลี่ยนไปใช้อย่างอื่นโดยไม่ได้บอก**

- หน้าเว็บ: **HTML / CSS / JavaScript ธรรมดา** — ไม่ใช้ framework ใด ๆ และไม่มีขั้นตอน build
- แยกเป็นไฟล์ `.html` หนึ่งไฟล์ต่อหนึ่งหน้าจอ · หน้าตาอยู่ใน `css/` · การทำงานของปุ่มอยู่ใน `js/`
- เปิดดูในเครื่อง: `npm run dev`
- ฐานข้อมูล: **Firestore** (เรียกผ่าน Firebase SDK แบบ CDN เวอร์ชัน **12.18.0** · โหลดในแท็ก `<script type="module">`)
  - ค่าตั้งค่า Firebase อยู่ใน `js/firebase.js` ไฟล์เดียว (ค่าชุดนี้ไม่ใช่ความลับ ขึ้น GitHub ได้)
  - ทุกหน้า import คำสั่ง Firestore จาก `./firebase.js` เท่านั้น **ห้าม import คนละเวอร์ชันเอง** ล็อกอินจะหายเงียบ ๆ
- ล็อกอิน: **Firebase Authentication** แบบอีเมล/รหัสผ่าน
- ยามเฝ้าข้อมูล: **Security Rules** ในไฟล์ `firestore.rules`
- นำขึ้นออนไลน์: **Firebase Hosting** (`firebase deploy`)
- ฟีเจอร์ AI ในระบบ: **OpenRouter** รุ่น `google/gemini-2.5-flash-lite` — เรียกผ่าน `js/ai.js` ที่เดียว
  - คีย์อยู่ในไฟล์ `js/ai-config.js` ที่ `.gitignore` กันไว้ · แบบฟอร์มคือ `js/ai-config.example.js`
- ไอคอน: SVG ในไฟล์ `js/icons.js` (`<span data-icon="...">` หรือ `ไอคอน("...")`) — **ไม่ใช้ emoji บนหน้าจอ**
- ตารางสิทธิ์: `ACL.md` · กฎจริงที่ฐานข้อมูล: `firestore.rules` — สองไฟล์นี้ต้องตรงกันเสมอ

**ไม่มีเซิร์ฟเวอร์ที่เขียนเอง** — หน้าเว็บคุยกับ Firestore ตรง ๆ

---

## 3. หน้าจอของระบบ

| ไฟล์ | หน้าจอ | ทำอะไรได้ |
|---|---|---|
| `index.html` | หน้าแรก | รวมลิงก์ไปทุกหน้า และอธิบายบทบาทกับสถานะ |
| `login.html` | เข้าสู่ระบบ / สมัครสมาชิก | สมัครแล้วสร้างไฟล์ใน `users/` ให้อัตโนมัติ |
| `leave-requests.html` | รายการใบลา | ตารางใบลาพร้อมป้ายสถานะสี กดที่แถวไปหน้ารายละเอียด |
| `new-leave-request.html` | ยื่นใบลาใหม่ | ฟอร์มกรอกใบลา และปุ่มให้ AI ช่วยจัดประเภทการลา (AI ระดับ 1) |
| `leave-request-detail.html` | รายละเอียดใบลา | ดูครบทุกช่อง · อนุมัติ / ไม่อนุมัติ · เขียนความเห็น · ลบ · ให้ AI สรุปใบลา (AI ระดับ 2) |
| `leave-types.html` | จัดการประเภทการลา | เพิ่ม แก้ ลบ ประเภทการลา |
| `dashboard.html` | แดชบอร์ดสรุป | กล่องตัวเลข 3 กล่องตามสถานะ และใบลา 5 รายการล่าสุด |
| `seed.html` | ใส่ข้อมูลตัวอย่าง | กดปุ่มครั้งเดียวเพื่อสร้างข้อมูลตั้งต้นบน Firestore |

---

## 4. โครงสร้างข้อมูลบน Firestore

> โฟลเดอร์ = Collection · ไฟล์ = Document · ข้อมูลในไฟล์ = Field

```
users/
   u001  { name, email, role }

leaveTypes/
   lt001 { name }

leaveRequests/
   lr001 { title, reason, status,
              requesterId,  requesterName,
              approverId,   approverName,
              leaveTypeId,  leaveTypeName,
              startDate, endDate, createdAt,
              aiSuggestion }                 ← สรุปจาก AI (สัปดาห์ที่ 8)
      approvals/
         ap001 { authorId, authorName, message, createdAt }
      aiLog/                            ← บันทึกทุกครั้งที่เรียก AI สรุป (สัปดาห์ที่ 8)
         (รหัสสุ่ม) { input, output, createdAt, createdBy, model }
```

**ข้อมูลที่จดซ้ำไว้ (denormalize) และเหตุผล**

Firestore ไม่มี JOIN ถ้าเก็บแต่รหัส หน้าจอจะขึ้นว่า `u001` แทนชื่อคน และต้องเปิดอีกโฟลเดอร์ต่อหนึ่งแถว

| ช่องที่จดซ้ำ | จดมาจากไหน | ทำไมต้องจดซ้ำ |
|---|---|---|
| `leaveRequests.requesterName` | `users.name` | หน้ารายการต้องแสดงชื่อผู้ขอลาทุกแถว |
| `leaveRequests.approverName` | `users.name` | แสดงผู้อนุมัติโดยไม่ต้องเปิดโฟลเดอร์ `users` |
| `leaveRequests.leaveTypeName` | `leaveTypes.name` | ใช้แสดงและใช้กรองในหน้ารายการ |
| `approvals.authorName` | `users.name` | รายการความเห็นต้องขึ้นชื่อคนเขียนทุกบรรทัด |

**สถานะที่เป็นไปได้:** `รอพิจารณา` → `อนุมัติ` หรือ `ไม่อนุมัติ` (ทั้งสองค่าหลังเป็นปลายทาง เปลี่ยนต่อไม่ได้)

---

## 5. ข้อตกลงของโครงงานนี้

- ข้อความบนหน้าจอทั้งหมดเป็น **ภาษาไทย** · คอมเมนต์ในโค้ดเป็นภาษาไทย
- ชื่อ field ใน Firestore **ต้องสะกดตรงตามหัวข้อ 4 เป๊ะ** — Firestore แยกตัวพิมพ์เล็ก-ใหญ่
  `status` กับ `Status` คือคนละช่อง และจะทำให้ระบบพังแบบเงียบ ๆ
- ช่อง `status` ใช้ได้ **3 ค่าเท่านั้น** — ใบลาที่สร้างใหม่ตั้งเป็น `รอพิจารณา` เสมอ ไม่ให้ผู้ใช้เลือกเอง
- เปลี่ยนสถานะ ให้แก้เฉพาะช่อง `status` **ห้ามเขียนทับช่องอื่นในไฟล์เดิม**
- การเปลี่ยนเป็น `ไม่อนุมัติ` ต้องมีความเห็นในโฟลเดอร์ย่อย `approvals` อย่างน้อย 1 รายการก่อน
- ลบใบลาได้เฉพาะใบที่สถานะยังเป็น `รอพิจารณา` และต้องถามยืนยันก่อนเสมอ
- `createdAt` เก็บเป็น**ข้อความ**รูปแบบ `ปี-เดือน-วัน ชั่วโมง:นาที` เช่น `2026-09-01 09:15`
- ทุกหน้าจอต้องใช้บนมือถือได้
- ทุกจุดที่ต้องรอ ต้องมีข้อความบอกว่ากำลังโหลดอยู่
- ข้อความแจ้งข้อผิดพลาดต้องเป็นภาษาไทยที่บอกว่า **เกิดอะไรขึ้น และควรทำอย่างไรต่อ**
- ตัวเลขบนแดชบอร์ดต้องนับจากข้อมูลจริงเสมอ **ห้ามใส่ตัวเลขค้างไว้ในโค้ด**
- ทุกหน้าต้อง **รอสถานะล็อกอินพร้อมก่อน** แล้วค่อยอ่านข้อมูลจาก Firestore
- AI **เสนอ** เท่านั้น ไม่ตัดสินแทน — ทุกผลจาก AI ต้องมีป้าย "ข้อเสนอจาก AI — โปรดตรวจสอบก่อนยืนยัน"
  และโค้ดฝั่ง AI **ห้ามแตะช่อง `status`** (กฎใน `firestore.rules` ก็ไม่ยอมเช่นกัน)
- เรียก AI รอได้ไม่เกิน 15 วินาที · พังหรือช้า ระบบต้องไม่ค้าง และปุ่มบันทึกยังกดได้
- แก้ทีละอย่าง แล้วให้ผมตรวจก่อนทำต่อ

---

## 6. สิ่งที่ห้ามทำ

- **ห้ามเปลี่ยนไปใช้ framework** (React / Vue / Vite / Tailwind ฯลฯ)
- **ห้ามใส่ API key หรือรหัสผ่านลงในโค้ดที่จะ commit** — ต้องอยู่ในไฟล์ที่ `.gitignore` กันไว้
- **ห้ามใส่ข้อมูลส่วนบุคคลจริงของใคร** — ใช้ชื่อสมมติ (สมชาย · สมหญิง · สมศรี) และอีเมล `@example.com` เสมอ
- **ห้ามส่งเหตุผลการลาที่มีข้อมูลส่วนบุคคลจริงไปให้ AI ภายนอก**
- ห้ามเปิด Security Rules ให้กว้างเพื่อให้ทดสอบง่ายขึ้น
- ห้ามลบหรือแก้ไฟล์ที่ผมไม่ได้ขอ
- ห้ามเปลี่ยนชื่อ field ที่มีอยู่แล้วโดยไม่บอกผมก่อน

---

## 7. สิ่งที่ยังไม่ทำในเดือนนี้

ยกไป Backlog Sprint 2 ทั้งหมด — **ห้ามสร้างขึ้นมาเองแม้จะเห็นว่าระบบควรมี**

- แดชบอร์ดที่มีกราฟ
- แนบเอกสารประกอบใบลา (ต้องใช้ Firebase Storage)
- ค้นหา กรองหลายเงื่อนไข และแบ่งหน้า (pagination)
- แจ้งเตือนทางอีเมล LINE หรือ SMS
- คำนวณวันลาคงเหลือหรือโควตาวันลาประจำปี
- ปฏิทินการลาของทีม และการตรวจวันลาชนกัน
- การอนุมัติหลายขั้น และสายบังคับบัญชาหลายชั้น
- ส่งออกรายงานเป็น Excel หรือ PDF

---

## 8. บันทึกสิ่งที่รู้ว่ายังไม่เรียบร้อย

- **คีย์ OpenRouter อยู่ในหน้าเว็บ คนอื่นเปิดดูได้** — ยอมรับได้เฉพาะในคาบเรียนเพราะผู้สอนตั้งวงเงินไว้
  ระบบจริงต้องเรียก AI ผ่านฝั่งเซิร์ฟเวอร์ · **จดไว้ใน `BACKLOG.md` (Sprint 2) แล้ว**
- ชื่อที่จดซ้ำไว้ (`requesterName` · `approverName` · `leaveTypeName`) จะไม่อัปเดตตาม
  ถ้ามีคนเปลี่ยนชื่อในโฟลเดอร์ `users` — เดือนนี้ยอมรับข้อจำกัดนี้
- หน้าแดชบอร์ดยังนับจากใบลาที่ดึงมาทั้งหมดในครั้งเดียว ถ้าใบลาเยอะมากจะช้า
- บทบาท `role` เปลี่ยนได้ด้วยมือใน Firebase Console เท่านั้น ยังไม่มีหน้าจอจัดการผู้ใช้
