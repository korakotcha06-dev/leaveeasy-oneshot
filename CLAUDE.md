# CLAUDE.md — คู่มือสำหรับคนที่มารับงานนี้ต่อ

อ่านไฟล์นี้ก่อนแตะโค้ดสักบรรทัด

โปรเจกต์นี้เป็น **งานเรียน** ไม่ใช่ระบบเชิงพาณิชย์ ข้อจำกัดหลายข้อในนี้ดูเหมือนล้าสมัยหรือดูเหมือน
ทำน้อยเกินไป — ทุกข้อมีเหตุผลและถูกตกลงไว้แล้วใน `leaveeasy-spec.md` หัวข้อ 9

**ลำดับความจริงของโปรเจกต์นี้**

1. `firestore.rules` — ความจริงเรื่องสิทธิ์ ถ้าเอกสารไหนขัดกับไฟล์นี้ เอกสารนั้นผิด
2. `leaveeasy-spec.md` — ใบสั่งงาน บอกว่าต้องมีอะไรและห้ามมีอะไร
3. `tests/` — ข้อตกลงที่ตรวจได้ เทสสีเขียวคือหลักฐาน ไม่ใช่คำพูด
4. `ACL.md` · `BACKLOG.md` · `README.md` · ไฟล์นี้ — คำอธิบายให้คนอ่าน

---

## ข้อห้าม (ผิดข้อใดข้อหนึ่งคือส่งงานไม่ผ่าน)

- **ห้ามใช้ framework** — ไม่มี React Vue Angular Next Tailwind หรืออะไรก็ตาม HTML + CSS + JavaScript ล้วน
- **ห้ามมีขั้นตอน build** — ไฟล์ที่เขียนคือไฟล์ที่เบราว์เซอร์โหลด ไม่มี bundler ไม่มี transpiler
- **ห้ามเขียนเซิร์ฟเวอร์ของเราเอง** — ไม่มี Express ไม่มี Node server ไม่มี Cloud Functions
  หน้าเว็บคุยกับ Firestore ตรง ๆ
- **ห้ามใช้ฐานข้อมูลแบบตาราง** และไม่มี migration
- **ห้ามเพิ่มฟีเจอร์ที่สเปกไม่ได้สั่ง** — ไม่มีแจ้งเตือนอีเมล/LINE/SMS · ไม่มี export Excel/PDF ·
  ไม่มีอนุมัติหลายขั้น · ไม่มีคำนวณโควตาวันลา · ไม่มีปฏิทินทีม · ไม่มีประวัติการแก้ไข ·
  ไม่มีบทบาทที่ 4 · ไม่มีหลายภาษา ไม่มีโหมดกลางคืน · ไม่มี pagination · ไม่มีไฟล์แนบ (Module 3)
- **ห้าม emoji** ทุกที่ — โค้ด คอมเมนต์ UI เอกสาร รายงาน
- **ห้ามใส่ข้อมูลจริงของคน** ใช้ชื่อสมมติและอีเมลตัวอย่างตามสเปกหัวข้อ 7 เท่านั้น
- **ห้ามผ่อน `firestore.rules` เพื่อให้อะไรสักอย่างทำงานได้ง่ายขึ้น** โดยเฉพาะ `seed.html`
  (เหตุผลเต็มอยู่ใน `BACKLOG.md` ข้อ 3)

ถ้าคิดว่าข้อไหนจำเป็นต้องมีจริง ๆ **ให้เขียนเป็นข้อเสนอแล้วรอคำสั่ง อย่าลงมือทำเอง**

---

## กฎพอร์ต 5050 — ข้อที่พลาดแล้วเสียหายจริง

```js
// js/firebase.js
export const useEmulator =
  location.hostname === "localhost" && location.port === "5050";
```

แอปต่อ **ฐานจำลอง** ก็ต่อเมื่อพอร์ตเป็น `5050` เป๊ะ ๆ เท่านั้น **พอร์ตอื่นทั้งหมดคุยกับฐานข้อมูลจริง
บน Firebase** รวมถึงพอร์ต 3000 ที่ `npm run dev` เปิด

ชุดทดสอบ **ล้างฐานข้อมูลทิ้งทั้งก้อน** ก่อนทุกเทส เพื่อให้ผลคงที่ **ถ้าเทสไปวิ่งที่พอร์ตอื่น
เท่ากับสั่งลบข้อมูลจริง** กันไว้สองชั้นแล้ว (`playwright.config.js` ตรึง `baseURL` และ `signInAs()`
ตรวจพอร์ตซ้ำอีกรอบ) **ห้ามถอดด่านใดด่านหนึ่งออก**

---

## emulator กับ Java

Firestore emulator เขียนด้วย Java เครื่องนี้ลง Java ผ่าน Homebrew ซึ่งเป็นแบบ **keg-only**
แปลว่า `java` ไม่อยู่ใน PATH ปกติ พิมพ์ `java -version` เฉย ๆ จะหาไม่เจอ

คำสั่งที่เปิด emulator ต้องนำหน้าด้วย

```bash
PATH="/opt/homebrew/opt/openjdk/bin:$PATH"
```

npm script ใส่ให้แล้วทุกตัว **ให้เรียกผ่าน npm script เสมอ อย่าพิมพ์ `firebase emulators:start` เอง**

| คำสั่ง | ทำอะไร |
|---|---|
| `npm run emulators` | เปิด emulator ค้างไว้ (hosting 5050 · firestore 8080 · auth 9099 · UI 4000) |
| `npm run test:e2e` | รันเทสหน้าจอ 64 เทส (ต้องมี emulator เปิดอยู่แล้ว) |
| `npm run test:rules` | รันเทสกฎ 112 ข้อยืนยัน |
| `npm run test:ci` | เปิด emulator รันเทส แล้วเก็บกวาดให้เองครบในคำสั่งเดียว |
| `npm run dev` | เปิดเว็บที่พอร์ต 3000 ซึ่ง **คุยกับฐานข้อมูลจริง** |

**ห้ามรัน `test:rules` ปนรอบเดียวกับ `test:e2e`** เครื่องมือทดสอบกฎ **อัปโหลดไฟล์กฎทับและล้างฐานข้อมูล**
ระหว่างรัน ให้ปิดแล้วเปิด emulator ใหม่คั่นกลาง หรือรันเทสกฎบนพอร์ต firestore คนละพอร์ตไปเลย

**อย่าให้ Playwright เปิด emulator ให้เอง** (`webServer`) เคยลองแล้วและตัดทิ้ง — Playwright ฆ่าได้แค่
โปรเซส `firebase` ที่เป็นแม่ ส่วน Firestore emulator เป็นโปรเซส Java ลูกที่รอดมาจับพอร์ต 8080 ค้างไว้
รอบถัดไปจะล้มด้วยข้อความที่ไม่บอกสาเหตุจริง

---

## โครงข้อมูลบน Firestore — ชื่อช่องต้องตรงทุกตัวอักษร

> Firestore มองตัวพิมพ์เล็ก-ใหญ่เป็นคนละช่อง `status` กับ `Status` คือคนละช่อง
> **พิมพ์ผิดจะไม่มี error ขึ้นเลย ข้อมูลจะหายไปเฉย ๆ** ลอกจากตารางนี้ อย่าพิมพ์จากความจำ

```
users/{uid}
    name          ข้อความ   ชื่อ-นามสกุลที่แสดงบนหน้าจอ
    email         ข้อความ   อีเมลที่ใช้ล็อกอิน
    role          ข้อความ   employee | manager | hr  (ไม่มีค่าอื่น)

leaveTypes/{id}
    name          ข้อความ   ชื่อประเภทการลา

leaveRequests/{id}
    title         ข้อความ   หัวข้อใบลา
    reason        ข้อความ   เหตุผลการลา
    status        ข้อความ   รอพิจารณา | อนุมัติ | ไม่อนุมัติ  (ค่าภาษาไทย สามค่านี้เท่านั้น)
    requesterId   ข้อความ   uid ของผู้ขอลา  ** ต้องเป็น uid จริงจาก Firebase Auth **
    requesterName ข้อความ   จดซ้ำจาก users.name
    approverId    ข้อความ   uid ของผู้อนุมัติ · เป็นสตริงว่างได้
    approverName  ข้อความ   จดซ้ำจาก users.name · เป็นสตริงว่างได้
    leaveTypeId   ข้อความ   ชื่อไฟล์ใน leaveTypes
    leaveTypeName ข้อความ   จดซ้ำจาก leaveTypes.name
    startDate     ข้อความ   รูปแบบ ปี-เดือน-วัน เช่น 2026-09-07
    endDate       ข้อความ   รูปแบบเดียวกัน
    createdAt     วันเวลา   ใช้ serverTimestamp() เสมอ ห้ามใช้นาฬิกาของเครื่องผู้ใช้

leaveRequests/{id}/approvals/{id}      โฟลเดอร์ย่อย
    authorId      ข้อความ   uid ของผู้เขียน ต้องเป็นตัวเองเท่านั้น
    authorName    ข้อความ   จดซ้ำจาก users.name
    message       ข้อความ   เนื้อความเห็น
    createdAt     วันเวลา   serverTimestamp()
```

หมายเหตุสองข้อ

- ชื่อโฟลเดอร์เขียนติดกันไม่มีขีดล่าง — `leaveRequests` และ `leaveTypes` ไม่ใช่ `leave_requests`
- **ไม่มีช่อง `attachmentUrl` จริงในข้อมูล** สเปกจองชื่อไว้แต่เป็นงาน Module 3 (`BACKLOG.md` ข้อ 8)

---

## เครื่องสถานะของใบลา

```
รอพิจารณา  ──►  อนุมัติ      (ปลายทาง เปลี่ยนต่อไม่ได้)
           └─►  ไม่อนุมัติ    (ปลายทาง เปลี่ยนต่อไม่ได้)
```

**กฎ 7 ข้อที่ต้องบังคับครบทุกข้อ**

1. ใบใหม่ทุกใบเริ่มที่ `รอพิจารณา` เสมอ · ระบบตั้งให้ ไม่ให้ผู้ใช้เลือก
2. `อนุมัติ` และ `ไม่อนุมัติ` เป็นปลายทาง แก้ซ้ำไม่ได้ ย้อนกลับไม่ได้
3. เปลี่ยนสถานะได้เฉพาะ `manager` และ `hr` · `employee` ทำไม่ได้
4. **แยกหน้าที่** — ห้ามทุกคนพิจารณาใบที่ตัวเองยื่น ต่อให้เป็น manager หรือ hr
5. การเปลี่ยนสถานะต้องแตะ **เฉพาะช่อง `status` ช่องเดียว** → ใช้ `updateDoc({ status })`
   **ห้ามใช้ `setDoc` เด็ดขาด** เพราะมันเขียนทับทั้งไฟล์และช่องอื่นจะหาย (กฎก็ปฏิเสธด้วย)
6. จะตั้ง `ไม่อนุมัติ` ต้องมีความเห็นใน `approvals` อย่างน้อย 1 รายการก่อน
   **ข้อนี้บังคับในเบราว์เซอร์เท่านั้น กฎบังคับไม่ได้** อ่าน `BACKLOG.md` ข้อ 2 ก่อนคิดจะแก้
7. ลบใบลาได้เฉพาะ **เจ้าของใบ** และเฉพาะตอนที่ยัง `รอพิจารณา` · ไม่มีบทบาทไหนลบใบของคนอื่นได้

---

## กติกาของโค้ด

### `js/data.js` เป็นไฟล์เดียวในระบบที่แตะ collection ของ Firestore

หน้าจอทุกหน้าเรียกฟังก์ชันจาก `data.js` **ห้าม import `collection` `doc` `getDocs` ฯลฯ ไปใช้ในไฟล์หน้าจอเอง**

เหตุผล: ชื่อช่องข้อมูลเป็นตัวพิมพ์เล็ก-ใหญ่สำคัญและพิมพ์ผิดแล้วพังเงียบ ถ้าให้ทุกหน้าเขียนคำสั่งเอง
ชื่อช่องจะกระจายอยู่ 6-7 ที่ · `data.js` ทำให้ชื่อช่องมีอยู่จริงที่เดียว

### `js/firebase.js` เป็นไฟล์เดียวที่รู้จัก URL ของ CDN

Firebase รุ่นโมดูลถือว่า "CDN หนึ่ง URL = ไลบรารีหนึ่งชุด" ถ้าแต่ละหน้า import คนละ URL หรือคนละเวอร์ชัน
เบราว์เซอร์จะโหลดไลบรารีหลายชุดที่มองไม่เห็นแอปของกันและกัน อาการคือหน้าหนึ่งล็อกอินติด อีกหน้าบอกว่า
ยังไม่ได้ล็อกอิน โดยไม่มี error ขึ้นเลย

**เวอร์ชันตรึงที่ `12.18.0`** จะอัปต้องแก้ทุกบรรทัด import พร้อมกัน

ข้อยกเว้นเดียวคือ `js/auth.js` ที่ import `firebase-auth.js` จาก CDN เอง แต่รับ `app` ที่สร้างแล้วจาก
`firebase.js` ไปใช้ ไม่ได้สร้างแอปใหม่

### กติกาอื่น

- หนึ่งหน้าจอ = หนึ่งไฟล์ `.html` + หนึ่งไฟล์ `.js` ชื่อเดียวกัน · สไตล์อยู่ที่ `css/style.css` ไฟล์เดียว
- ข้อความบนหน้าจอเป็นภาษาไทยทั้งหมด · ชื่อไฟล์และชื่อช่องข้อมูลเป็นอังกฤษตามตารางด้านบน
- คอมเมนต์ในโค้ดเขียนภาษาไทย อธิบาย **ว่าทำไม** ไม่ใช่ว่าทำอะไร เขียนให้คนเพิ่งเริ่มอ่านรู้เรื่อง
  (นี่คืองานเรียน โค้ดต้องอธิบายตัวเองได้)
- ข้อความที่มาจากผู้ใช้ต้องผ่าน `escapeHtml()` จาก `util.js` ก่อนแทรกลง `innerHTML` เสมอ
  (มีเทส XSS คุมข้อนี้อยู่)
- ค่าคงที่สถานะใช้ `STATUS.PENDING` / `STATUS.APPROVED` / `STATUS.REJECTED` จาก `util.js`
  **ห้ามพิมพ์สตริงไทยเองในไฟล์อื่น** พิมพ์ผิดตัวเดียวแล้วเทสจะเขียวทั้งที่ระบบพัง
- error จาก Firebase แปลเป็นภาษาไทยด้วย `describeError()` ก่อนแสดง ห้ามโยน error ดิบขึ้นหน้าจอ
- `data-testid` ในแต่ละหน้าคือสัญญากับชุดทดสอบ **เปลี่ยนชื่อเมื่อไรเทสพังทันที**
  รายการตัวจริงอยู่ที่ `tests/fixtures/config.js`

---

## เรื่องที่ `firestore.rules` บังคับไม่ได้ และใครบังคับแทน

| กฎ | บังคับที่ไหนจริง ๆ |
|---|---|
| ต้องมีความเห็นก่อนตั้ง `ไม่อนุมัติ` | `js/leave-request-detail.js` — กฎ Firestore นับเอกสารใน subcollection ไม่ได้ |
| วันเริ่มต้องไม่หลังวันสิ้นสุด | `js/new-leave-request.js` — ตรวจในฟอร์ม |
| ตั้งใครเป็น `manager` / `hr` | Firebase Console — ไม่มีหน้าจอในระบบ |

รายละเอียดอยู่ใน `ACL.md` หัวข้อ 9 และ `BACKLOG.md` ข้อ 2 กับข้อ 4

---

## ก่อนบอกว่าเสร็จ

1. รัน `npm run test:e2e` และ `npm run test:rules` ให้เขียวทั้งคู่ (คั่นกลางด้วยการเปิด emulator ใหม่)
2. ถ้าแตะ `firestore.rules` ให้ไล่เช็กลิสต์ท้าย `ACL.md` หัวข้อ 11 ด้วยตา แล้วอัปเดต `ACL.md` ให้ตรง
3. ถ้าแตะเรื่องที่อยู่ใน `BACKLOG.md` ให้แก้ข้อนั้นในเอกสารด้วย **อย่าปล่อยให้เอกสารเล่าเรื่องที่ไม่จริงแล้ว**
4. ถ้าจะปล่อยขึ้นเว็บจริง สั่ง deploy **ทั้ง hosting และ firestore:rules** แล้วเปิดเว็บจริงดูด้วยตา
   — "เทสผ่าน" ไม่ใช่หลักฐานว่าของบนเว็บจริงถูกต้อง

---

## บันทึกการส่งมอบ

ไฟล์ `contract.md` เคยอยู่ในโฟลเดอร์นี้ เป็นเอกสารย่อของสเปกที่ทำไว้ให้ agent หลายตัวทำงานขนานกัน
ระหว่างการสร้างระบบ (โครงข้อมูล · เครื่องสถานะ · รายการ `data-testid` · ข้อมูลตัวอย่าง)
**ถูกลบทิ้งตอนส่งมอบเพราะเป็นนั่งร้านชั่วคราว ไม่ใช่เอกสารของระบบ** คอมเมนต์ในโค้ดและในเทสบางบรรทัด
ยังอ้างถึง "contract หัวข้อ N" อยู่ ให้อ่านเป็นการอ้างถึงเนื้อหาที่ย้ายมาอยู่ในไฟล์นี้กับ
`tests/fixtures/config.js` แทน
