# BACKLOG — ข้อจำกัดที่รู้อยู่แล้วและจงใจยอมรับ

เอกสารนี้ **ไม่ใช่รายการบั๊ก** และไม่ใช่รายการงานที่ลืมทำ

ทุกข้อในนี้คือเรื่องที่ **เจอแล้ว เข้าใจสาเหตุแล้ว และตัดสินใจว่าจะไม่แก้ในเดือนนี้** พร้อมเหตุผล
บางข้อแก้ไม่ได้เลยด้วยเครื่องมือที่สเปกอนุญาต บางข้ออยู่นอกขอบเขตที่ตกลงกันไว้ในสเปกหัวข้อ 9

**อ่านเอกสารนี้ก่อนคิดจะ "แก้" อะไรในระบบ** ถ้าเจออาการที่ตรงกับข้อใดข้อหนึ่งในนี้ แปลว่าระบบทำงานถูกแล้ว
สิ่งที่ผิดคือความคาดหวัง ไม่ใช่โค้ด

แต่ละข้อเขียน 3 ส่วนเสมอ — **ข้อจำกัดคืออะไร · ทำไมถึงเป็นแบบนั้น · ทางแก้จริงต้องทำอะไร**

---

## 1. API key ของ OpenRouter หลุดถึงเบราว์เซอร์

**ข้อจำกัดคืออะไร**

ปุ่ม "ให้ AI ช่วยจัดประเภทการลา" ในหน้า `new-leave-request.html` เรียก OpenRouter จากเบราว์เซอร์ของผู้ใช้
โดยตรง โดยแนบ API key ที่อ่านมาจาก `js/ai-config.js` ไปกับคำขอ

ไฟล์ `js/ai-config.js` อยู่ใน `.gitignore` จึงไม่ขึ้น git — **แต่นั่นกันแค่ไม่ให้ key รั่วทาง repo**
ถ้าเว็บถูก deploy พร้อมไฟล์นี้ มันจะถูกส่งไปให้เบราว์เซอร์เหมือนไฟล์ JavaScript อื่น ใครเปิด devtools ก็เห็น key
คัดลอกไปยิง OpenRouter ในนามเราได้ทันที ไม่มีเพดานการใช้งาน ไม่มีการจำกัดจำนวนครั้ง

**สิ่งที่ตัดสินใจไปแล้วสำหรับเดือนนี้ — ไม่เอา key ขึ้นเว็บจริงเลย**

`js/ai-config.js` ถูกใส่ไว้ในช่อง `hosting.ignore` ของ `firebase.json` แล้ว จึงไม่ถูกอัปขึ้น Firebase Hosting
ไม่ว่าจะสั่ง deploy กี่ครั้ง · ตรวจจากเว็บจริงแล้วว่า `/js/ai-config.js` ตอบ HTTP 404

ผลที่ตามมาโดยตั้งใจ คือ **ปุ่มผู้ช่วย AI ใช้ได้เฉพาะตอนเปิดเว็บในเครื่องเท่านั้น**
บนเว็บจริงปุ่มจะขึ้นข้อความภาษาไทยว่ายังไม่ได้ตั้งค่า AI แล้วให้เลือกประเภทการลาเอง
ซึ่งเป็นพฤติกรรมที่ `js/ai.js` ออกแบบรองรับไว้แล้วและมีเทสคุมอยู่ 5 ข้อ

ถ้าวันไหนตัดสินใจจะให้ปุ่มนี้ทำงานบนเว็บจริง ต้องถอดบรรทัดนั้นออกจาก `firebase.json` ก่อน
และต้องยอมรับว่า key ก้อนนั้นถือว่าเปิดเผยต่อสาธารณะทันทีที่ deploy

**ทำไมถึงเป็นแบบนั้น**

สเปกหัวข้อ 9 ห้ามเขียนเซิร์ฟเวอร์ของตัวเอง ห้าม Express ห้าม Node server ห้าม Cloud Functions
เมื่อไม่มีเครื่องกลางที่เชื่อถือได้เป็นคนถือ key ก็ไม่มีที่ให้เก็บ key นอกจากฝั่งเบราว์เซอร์
นี่คือข้อจำกัดของขอบเขตงาน ไม่ใช่ความไม่รู้

(คนละเรื่องกับ `firebaseConfig` ใน `js/firebase.js` ซึ่งเป็นคีย์ฝั่งเบราว์เซอร์โดยการออกแบบ
ไม่ใช่ความลับ และตัวที่กันข้อมูลจริงคือ `firestore.rules`)

**ทางแก้จริงต้องทำอะไร**

มีเซิร์ฟเวอร์ของเราเองคั่นกลาง key เก็บไว้ที่เซิร์ฟเวอร์อย่างเดียว เบราว์เซอร์ยิงมาที่เซิร์ฟเวอร์ของเรา
เซิร์ฟเวอร์ตรวจว่าคนยิงล็อกอินอยู่จริง (ตรวจ Firebase ID token) จำกัดจำนวนครั้งต่อผู้ใช้ต่อช่วงเวลา
แล้วค่อยยิงต่อไปที่ OpenRouter · เป็นเนื้อหาของ Module 3 ขึ้นไป

**ทางลดความเสียหายที่ทำได้ทันทีโดยไม่ผิดสเปก** ตั้งเพดานวงเงินของ key นั้นไว้ต่ำ ๆ ที่ฝั่ง OpenRouter
และหมุน key ใหม่หลังถ่ายคลิปสาธิตเสร็จ

---

## 2. กฎ "ต้องมีความเห็นก่อนกดไม่อนุมัติ" บังคับในเบราว์เซอร์เท่านั้น และบังคับใน security rules ไม่ได้เลย

**ข้อจำกัดคืออะไร**

สเปกหัวข้อ 6 กำหนดว่า การเปลี่ยนสถานะเป็น `ไม่อนุมัติ` ต้องมีความเห็นในโฟลเดอร์ย่อย `approvals`
อย่างน้อย 1 รายการก่อน

กฎข้อนี้บังคับอยู่ที่ `js/leave-request-detail.js` ซึ่งเป็นโค้ดฝั่งเบราว์เซอร์ **`firestore.rules` บังคับข้อนี้ไม่ได้**
ใครก็ตามที่เปิด console แล้วสั่ง Firestore ตรง ๆ จะตั้ง `ไม่อนุมัติ` ได้โดยไม่ต้องเขียนความเห็นเลย
(ต้องเป็น `manager` หรือ `hr` และต้องไม่ใช่ใบของตัวเองอยู่ดี — สี่ด่านที่เหลือยังบังคับครบ)

**ทำไมถึงเป็นแบบนั้น**

นี่คือ **ข้อจำกัดของตัวภาษา security rules ไม่ใช่เรื่องเขียนกฎเก่งไม่พอ**

ภาษาของ Firestore rules เปิดดูเอกสารอื่นได้เฉพาะเอกสารที่ **รู้เส้นทางเต็มเป๊ะ ๆ อยู่แล้ว** ผ่าน
`exists()` และ `get()` เท่านั้น มัน **สั่งค้นหาทั้งโฟลเดอร์ไม่ได้** ไม่มีคำสั่งนับจำนวนเอกสารใน
subcollection ไม่มีทางถามว่า "โฟลเดอร์ย่อย approvals ของใบนี้มีกี่รายการ" เพราะชื่อเอกสารความเห็น
ถูกสุ่มโดย Firestore จึงไม่มีใครรู้ล่วงหน้าว่าต้องไป `exists()` ที่เส้นทางไหน

> **ห้ามหาทางอ้อมให้บังคับได้ และห้ามผ่อนกฎข้ออื่นเพื่อแลกกับเรื่องนี้** — ข้อนี้บันทึกไว้แล้วใน
> `ACL.md` หัวข้อ 9 เช่นกัน ทางอ้อมที่ดูเหมือนจะได้ผล (เช่น ให้หน้าเว็บเขียนช่อง `hasComment: true`
> ลงใบลาแล้วให้กฎอ่านช่องนั้น) ไม่ได้แก้อะไรเลย เพราะช่องนั้นก็มาจากเบราว์เซอร์ของคนเดิมอยู่ดี

**ทางแก้จริงต้องทำอะไร**

ต้องมีตรรกะฝั่งเซิร์ฟเวอร์เป็นคนเปลี่ยนสถานะให้ — Cloud Function ที่รับคำสั่ง "ไม่อนุมัติใบนี้"
แล้วมันเป็นคนไปนับความเห็นเองด้วยสิทธิ์ admin ก่อนจะเขียน `status` จากนั้นปิดสิทธิ์เขียน `status`
ของผู้ใช้ทั่วไปทิ้งทั้งหมด ให้เหลือทางเดียวคือผ่านฟังก์ชันนั้น · สเปกหัวข้อ 9 ห้าม Cloud Functions
เดือนนี้จึงจบที่ "บังคับในเบราว์เซอร์และบันทึกไว้ว่ารู้"

---

## 3. `seed.html` ใช้งานไม่ได้อีกแล้วเมื่อกฎความปลอดภัยเปิดใช้งาน

**ข้อจำกัดคืออะไร**

`seed.html` กับ `js/seed.js` เขียนไว้ตั้งแต่สัปดาห์ที่ 6 ตอนที่ยังไม่มีกฎ กดปุ่มแล้วมันเขียนข้อมูลตัวอย่าง
ตามสเปกหัวข้อ 7 ลงฐานข้อมูลให้ทั้งชุด

ตั้งแต่สัปดาห์ที่ 8 เป็นต้นมา **ปุ่มนี้จะถูกปฏิเสธเกือบทุกบรรทัด** เพราะข้อมูลตัวอย่างประกอบด้วย

- ไฟล์ `users/u002` ที่มี `role: "manager"` และ `users/u003` ที่มี `role: "hr"` — กฎยอมให้เขียนโปรไฟล์
  ได้เฉพาะไฟล์ที่ชื่อตรงกับ uid ของตัวเอง และบังคับ `role` เป็น `employee` เท่านั้น
- ใบลาของคนอื่น (`requesterId` ไม่ใช่ uid ของคนที่กดปุ่ม)
- ใบลาที่มีสถานะ `อนุมัติ` และ `ไม่อนุมัติ` มาตั้งแต่ตอนสร้าง — กฎบังคับว่าใบใหม่ทุกใบต้องเริ่มที่ `รอพิจารณา`

**ทำไมถึงเป็นแบบนั้น และทำไมถึงไม่แก้**

**กฎทำงานถูกแล้ว นี่ไม่ใช่บั๊ก** ถ้าผ่อนกฎให้ `seed.html` กดผ่าน ก็คือเปิดรูให้ผู้ใช้คนไหนก็ได้
เขียนไฟล์ `users/u003` ที่มี `role: "hr"` — รูที่เจาะไว้ให้ seed คือรูเดียวกับที่คนนอกใช้ตั้งตัวเองเป็น hr
**จึงจงใจไม่แก้กฎเพื่อรองรับ seed** (บันทึกไว้ตรงกันใน `ACL.md` หัวข้อ 10)

**ทางที่ถูกต้องคือ seed ผ่านช่องทางผู้ดูแลระบบ ซึ่งข้าม `firestore.rules` โดยการออกแบบ**

| จะใส่ข้อมูลที่ไหน | ใช้ทางนี้ |
|---|---|
| ฐานจำลอง ตอนรันเทสอัตโนมัติ | `tests/fixtures/emulator.js` ยิง REST ของ emulator ด้วย `Authorization: Bearer owner` · ใช้อยู่จริงและผ่านทุกเทส |
| ฐานจำลอง ตอนอยากดูด้วยตา | คำสั่งใน README หัวข้อ "ใส่ข้อมูลตัวอย่าง" หรือ Emulator UI ที่ http://localhost:4000 |
| ฐานข้อมูลจริงบน Firebase | Firebase Console เขียนในฐานะเจ้าของโปรเจกต์ · ที่เดียวกับที่ใช้ตั้ง `role` อยู่แล้ว |

**ทางแก้จริงต้องทำอะไร**

เขียนสคริปต์ seed ที่รันด้วย Firebase Admin SDK ฝั่งเครื่อง (ไม่ใช่ในเบราว์เซอร์) ซึ่งถือ service account
และข้ามกฎได้อย่างถูกต้อง แล้ว **ลบ `seed.html` ทิ้ง** เพราะหน้าเว็บที่ผู้ใช้เปิดได้ไม่ควรมีปุ่มแบบนี้ตั้งแต่แรก
เดือนนี้เก็บไฟล์ไว้เป็นหลักฐานของสัปดาห์ที่ 6 และบันทึกสถานะไว้ตรงนี้แทน

---

## 4. รหัสผู้ใช้ `u001`–`u003` ในข้อมูลตัวอย่าง ไม่มีวันตรงกับ uid จริงของ Firebase Auth

**ข้อจำกัดคืออะไร**

สเปกหัวข้อ 7 กำหนดชื่อไฟล์ผู้ใช้ไว้ว่า `u001` `u002` `u003` แต่ของจริง **Firebase Auth เป็นคนแจก uid ให้เอง**
เป็นสตริงสุ่มยาว ๆ และกฎความปลอดภัยเทียบตรง ๆ ว่า `requesterId == request.auth.uid`

ผลคือใบลาที่ seed ด้วย `requesterId: "u001"` ตามสเปกเป๊ะ ๆ จะไม่ถูกมองว่าเป็นของใครเลย
คนที่ล็อกอินจริงเปิดดูไม่ได้สักใบ · `tests/fixtures/` จึงตั้ง uid ทดสอบเป็น `uid-employee-u001`
`uid-manager-u002` `uid-hr-u003` แทน (อธิบายไว้ใน `tests/README.md`)

เรื่องที่ตามมาและสำคัญกว่า: **คนจริงที่สมัครเข้าระบบจะได้ `role: "employee"` เสมอ** และ
**ไม่มีหน้าจอไหนในระบบที่เปลี่ยน `role` ได้** การตั้งใครเป็น `manager` หรือ `hr` ต้องเข้า Firebase Console
แล้วแก้ค่าในไฟล์ `users/{uid}` ด้วยมือทีละคน

**ทำไมถึงเป็นแบบนั้น**

บังคับสองชั้นและตั้งใจทั้งคู่ — `js/data.js` เขียน `role: "employee"` ตายตัวตอนสมัคร (ไม่รับค่าจากหน้าจอเลย)
และ `firestore.rules` ก็ปฏิเสธการสมัครที่ขอ `role` อื่นอยู่แล้ว ถ้ามีหน้าจอให้ตั้งบทบาทเองได้
ใครก็ตั้งตัวเองเป็น hr ได้ ซึ่งทำให้ระบบสิทธิ์ทั้งชุดไร้ความหมาย

**ทางแก้จริงต้องทำอะไร**

หน้าจอจัดการผู้ใช้สำหรับ hr ที่เปลี่ยน `role` ของคนอื่นได้ ซึ่งต้องมาคู่กับกฎที่ยอมให้ hr เขียนช่อง `role`
ของคนอื่น — และกฎแบบนั้นมาพร้อมคำถามที่ตอบยากขึ้นทันที เช่น hr ลดบทบาทของ hr คนอื่นได้ไหม
เหลือ hr คนสุดท้ายในระบบแล้วถอดตัวเองได้ไหม · ทางที่ปลอดภัยกว่าในระบบจริงคือใช้ **custom claims**
ของ Firebase Auth ซึ่งตั้งได้จากฝั่งเซิร์ฟเวอร์เท่านั้น · ทั้งสองทางอยู่นอกขอบเขตเดือนนี้
(สเปกหัวข้อ 9 ไม่มีหน้าจัดการผู้ใช้ และห้ามเขียนเซิร์ฟเวอร์)

---

## 5. ชื่อที่จดซ้ำไว้ไม่ไล่อัปเดตย้อนหลัง

**ข้อจำกัดคืออะไร**

ใบลาแต่ละใบเก็บชื่อคนซ้ำไว้ในตัวเอง — `requesterName` `approverName` `leaveTypeName` และความเห็นเก็บ
`authorName` ถ้ามีคนแก้ `name` ในไฟล์ `users/{uid}` **ใบลาเก่าทุกใบยังขึ้นชื่อเดิม** เหมือนกัน
ถ้าเปลี่ยนชื่อประเภทการลาในหน้า `leave-types.html` ใบลาเก่าที่ใช้ประเภทนั้นยังขึ้นชื่อเก่า

**ทำไมถึงเป็นแบบนั้น**

Firestore ไม่มี JOIN ถ้าเก็บแต่รหัส หน้ารายการจะขึ้นว่า `uid-...` แทนชื่อคน และต้องยิงอ่านโฟลเดอร์ `users`
เพิ่มอีกหนึ่งครั้งต่อหนึ่งแถว ซึ่งช้าและเปลืองโควตาการอ่าน · สเปกหัวข้อ 5.3 ออกแบบให้จดซ้ำไว้ตั้งแต่ต้น
และ **ระบุชัดว่าเดือนนี้ยอมรับราคาข้อนี้ ไม่ต้องเขียนระบบไล่อัปเดต**

**ทางแก้จริงต้องทำอะไร**

ในระบบจริงมีสองทาง เลือกทางใดทางหนึ่ง

1. **ไล่อัปเดตตอนเขียน (fan-out)** — เมื่อชื่อในไฟล์ `users` เปลี่ยน ให้มีงานเบื้องหลังไล่เขียนทับ
   `requesterName` ทุกใบที่อ้างถึง uid นั้น · ต้องทำฝั่งเซิร์ฟเวอร์เพราะอาจแตะเอกสารหลายพันใบ
   และต้องทำเป็น batch ที่ล้มแล้วทำซ้ำได้
2. **ยอมรับว่าเป็นค่า ณ เวลานั้น** — ถือว่าชื่อที่จดไว้คือ "ชื่อของคนคนนั้น ณ วันยื่นใบลา" ซึ่งเป็นสิ่งที่
   เอกสารทางการมักต้องการจริง ๆ อยู่แล้ว แล้วเขียนกำกับบนหน้าจอให้ชัด

ทางที่ 1 ต้องมีเซิร์ฟเวอร์ · ทางที่ 2 เป็นการตัดสินใจเชิงผลิตภัณฑ์ ไม่ใช่เรื่องเทคนิค

---

## 6. ผู้อนุมัติคนเดียวในองค์กร ทำให้ใบลาของตัวเองค้างที่ `รอพิจารณา` ตลอดไป

**ข้อจำกัดคืออะไร**

กฎ **แยกหน้าที่ (separation of duties)** ห้ามทุกคนพิจารณาใบลาที่ตัวเองยื่น ไม่ว่าจะเป็นบทบาทอะไร
`manager` ที่ลาเอง และ `hr` ที่ลาเอง ก็กดอนุมัติใบของตัวเองไม่ได้ ทั้งบนหน้าจอ (ปุ่มถูกปิดพร้อมคำอธิบาย)
และในกฎ (`resource.data.requesterId != request.auth.uid`)

ผลคือ **ถ้าองค์กรมีผู้อนุมัติอยู่คนเดียว ใบลาของคนนั้นจะค้างที่ `รอพิจารณา` ถาวร** ไม่มีใครในระบบกดให้ได้

**ทำไมถึงเป็นแบบนั้น**

**นี่คือผลที่ตั้งใจ ไม่ใช่บั๊ก** คนเซ็นอนุมัติการลาให้ตัวเองคือช่องโหว่ทางการควบคุมภายใน ไม่ใช่ความสะดวก
สเปกหัวข้อ 1 ระบุไว้ว่าที่ทำงานเป้าหมายมีหัวหน้าผู้อนุมัติ **1–3 คน** ซึ่งกรณี 2 คนขึ้นไปไม่มีปัญหานี้
กรณีมีคนเดียวคือกรณีที่สเปกยอมรับว่าเกิดได้ และคำตอบคือเพิ่มผู้อนุมัติคนที่สอง ไม่ใช่ผ่อนกฎ

**ทางแก้จริงต้องทำอะไร**

- ทางองค์กร — มีผู้อนุมัติอย่างน้อย 2 คน (ทำได้ทันทีโดยไม่ต้องแก้โค้ด แค่ตั้ง `role` ให้อีกคนใน Console)
- ทางระบบ — ถ้าจำเป็นต้องรองรับองค์กรที่มีผู้อนุมัติคนเดียวจริง ๆ ต้องออกแบบ "ผู้อนุมัติสำรอง"
  หรือสายบังคับบัญชาหลายชั้น ซึ่ง **สเปกหัวข้อ 9 ตัดออกจากขอบเขตไว้ชัดเจนแล้ว**
  (ไม่มีการอนุมัติหลายขั้นหรือสายบังคับบัญชาหลายชั้น)

---

## 7. ไม่มีหน้าจอให้ hr กำหนดผู้อนุมัติของใบลา ทั้งที่ข้อมูลกับกฎรองรับไว้แล้ว

**ข้อจำกัดคืออะไร**

สเปกหัวข้อ 2 เขียนว่า `hr` "กำหนดผู้อนุมัติให้แต่ละใบ" ได้ และโครงข้อมูลก็มีช่อง `approverId` กับ
`approverName` รองรับไว้ครบ · **แต่ในระบบจริงไม่มีหน้าจอไหนที่ตั้งค่าสองช่องนี้ได้เลยหลังจากใบลาถูกสร้าง**

ค่าของสองช่องนี้ถูกกำหนดครั้งเดียวตอนสร้างใบลา และหน้า `new-leave-request.html` ไม่ได้ให้เลือก
ผู้อนุมัติ ใบลาที่ยื่นผ่านหน้าเว็บจึงมี `approverId` เป็นสตริงว่าง และหน้ารายละเอียดขึ้นว่า
"ยังไม่ได้กำหนดผู้อนุมัติ" (มีเทสคุมข้อความนี้ไว้)

ที่สำคัญคือ **ช่อง `approverId` ไม่ได้มีผลต่อสิทธิ์อะไรเลย** — ใครก็ตามที่เป็น `manager` หรือ `hr`
และไม่ใช่ผู้ยื่นใบนั้น พิจารณาใบนั้นได้ทุกใบ ไม่ว่า `approverId` จะเขียนว่าใคร ช่องนี้เป็นข้อมูลสำหรับแสดงผล
ล้วน ๆ

**ทำไมถึงเป็นแบบนั้น**

`firestore.rules` **ล็อกช่องอื่นทั้งหมดไว้หมด** การเขียนทับใบลาที่มีอยู่แล้วต้องเปลี่ยนแค่ช่อง `status`
ช่องเดียวเท่านั้น (ด่านที่ 4 ใน `ACL.md` หัวข้อ 6.2) — ด่านนี้มีไว้กันไม่ให้ใครแอบแก้ `reason` หรือ
`startDate` แนบไปกับการกดอนุมัติ **การเปิดให้แก้ `approverId` ได้ ต้องเจาะด่านนี้** ซึ่งเป็นด่านที่มีเทส
คุมอยู่หลายข้อ และสเปกหัวข้อ 4 ก็ไม่ได้ระบุว่าหน้าไหนมีปุ่มกำหนดผู้อนุมัติ · จึงเลือกไม่เจาะ

**ทางแก้จริงต้องทำอะไร**

เพิ่มข้อยกเว้นในกฎให้ `hr` เขียนได้เฉพาะคู่ `approverId` + `approverName` และเฉพาะตอนที่ใบยังเป็น
`รอพิจารณา` โดยต้องไม่แตะช่องอื่นเลย (เขียนแบบเดียวกับด่านที่ 4 ของ `status` แต่คนละชุดช่อง)
แล้วเพิ่มปุ่มบนหน้ารายละเอียดที่เห็นเฉพาะ hr · ต้องมาพร้อมเทสกฎชุดใหม่ที่พิสูจน์ว่า
`manager` ทำไม่ได้ · ผู้ยื่นทำไม่ได้ · และแก้พร้อมช่องอื่นไม่ได้

---

## 8. สิ่งที่สเปกเลื่อนไป Module 3 ด้วยตัวเอง

สามข้อนี้ไม่ใช่การตัดสินใจของคนทำ แต่เป็นขอบเขตที่ `leaveeasy-spec.md` กำหนดมาตั้งแต่ต้น
(หัวข้อ 4 และหัวข้อ 9) **ห้ามทำล่วงหน้า**

| เรื่อง | สถานะจริงตอนนี้ | ทางแก้จริง |
|---|---|---|
| **กราฟบนแดชบอร์ด** | `dashboard.html` นับตัวเลข 3 กล่องและใบลาล่าสุด 5 รายการ **จากข้อมูลจริง** (และนับเฉพาะที่ผู้ใช้คนนั้นมีสิทธิ์เห็น) แต่ **ไม่มีกราฟสักอัน** — ตัวเลขล้วน | ต่อไลบรารีกราฟ ซึ่งแปลว่าต้องเพิ่ม dependency ฝั่งเบราว์เซอร์ตัวแรกของโปรเจกต์ · Module 3 |
| **แนบไฟล์เอกสาร (US-12)** | ไม่มีปุ่มแนบ ไม่มีการอัปโหลด **และไม่มีช่อง `attachmentUrl` ในเอกสารใบลาจริง** แม้สเปกหัวข้อ 5.2 จะจองชื่อช่องไว้แล้ว | ต้องใช้ Firebase Storage ซึ่งเป็นบริการที่สี่ที่โปรเจกต์นี้ยังไม่ได้เปิดใช้ พร้อมกฎความปลอดภัยของ Storage อีกชุดหนึ่งแยกจาก `firestore.rules` · Module 3 |
| **แบ่งหน้ารายการ (pagination)** | หน้ารายการดึงใบลาทั้งหมดที่ผู้ใช้เห็นได้มาทีเดียว แล้วค้นหา/กรอง/เรียงในเบราว์เซอร์ · ใช้ได้ดีกับข้อมูลระดับสิบถึงร้อยใบ **ถ้าข้อมูลโตถึงระดับพันใบ หน้านี้จะช้าลงเรื่อย ๆ อย่างเงียบ ๆ** | ใช้ `limit()` + `startAfter()` ของ Firestore และย้ายการกรองไปเป็นเงื่อนไขของคำสั่งค้นหาแทนการกรองในเบราว์เซอร์ ซึ่งต้องสร้าง composite index ตามมา · สเปกหัวข้อ 9 สั่งไว้ว่าเดือนนี้ให้จำกัดจำนวนที่ดึงมาแสดงแทน |

---

## 9. ฟีเจอร์ AI ทำจริงแค่ครึ่งเดียวของที่สเปกสัปดาห์ที่ 8 เขียนไว้

**ข้อจำกัดคืออะไร**

สเปกหัวข้อ 8 แถวสัปดาห์ที่ 8 ระบุฟีเจอร์ AI ไว้ **สองอย่าง**

1. จัดประเภทการลาจากเหตุผล — **ทำแล้ว** อยู่ที่ `js/ai.js` (`classifyLeaveType`) และปุ่มในหน้า
   `new-leave-request.html`
2. สรุปใบลาให้หัวหน้าอ่านก่อนกดอนุมัติ — **ไม่ได้ทำ** ไม่มีฟังก์ชัน ไม่มีปุ่มในหน้ารายละเอียด

**ทำไมถึงเป็นแบบนั้น**

บันทึกตามความจริงว่าของไม่มี ไม่ใช่มีเหตุผลเชิงออกแบบมารองรับ · ข้อดีที่ตามมาโดยบังเอิญคือ
ข้อห้ามในสเปกหัวข้อ 9 ที่ว่า **ห้ามส่งเหตุผลการลาที่มีข้อมูลส่วนบุคคลจริงไปให้ AI ภายนอก** มีจุดเสี่ยง
เหลือจุดเดียวแทนที่จะเป็นสองจุด (ฟีเจอร์สรุปต้องส่งทั้งใบรวมความเห็นออกไป ซึ่งเสี่ยงกว่าการส่งแค่
ข้อความเหตุผลไปให้เลือกจากรายการสั้น ๆ มาก)

**ทางแก้จริงต้องทำอะไร**

เพิ่มฟังก์ชันสรุปใน `js/ai.js` และปุ่มในหน้ารายละเอียด โดยต้องแก้ข้อ 1 ของเอกสารนี้ก่อน
(key ยังหลุดอยู่) และต้องตัดสินใจเรื่องข้อมูลส่วนบุคคลให้ชัดก่อนว่าจะส่งอะไรออกไปบ้าง

---

## 10. เว็บจริงย้าย site แล้ว · ขึ้นครบทั้งหน้าเว็บและกฎ

**สถานะปัจจุบัน — ไม่เหลือข้อค้างในข้อนี้แล้ว**

โค้ดชุดนี้ขึ้น hosting site ใหม่ `leaveeasy-oneshot-korakot` แล้วที่
https://leaveeasy-oneshot-korakot.web.app ตรวจแล้วว่า `<title>` ตรงกับ `index.html`
และทั้ง 8 หน้าตอบ HTTP 200 · ชื่อ site ตั้งไว้ในช่อง `hosting.site` ของ `firebase.json`

`firestore.rules` ในโฟลเดอร์นี้ (ชุดเดียวกับที่ผ่านเทส 112 ข้อ) ขึ้นฐานข้อมูลจริงแล้วเช่นกัน
Firebase รายงานว่า compile ผ่านและ released เรียบร้อย

**ยืนยันกับของจริงแล้วอย่างไร**

ไม่ได้เชื่อข้อความ deploy อย่างเดียว — ยิง REST ของ Firestore เข้าฐานข้อมูลจริงแบบ **ไม่ล็อกอิน**
ไปที่ `leaveRequests` `users` และ `leaveTypes` ทั้งสามได้ `HTTP 403 PERMISSION_DENIED`
ตรงตามกฎข้อแรกที่ว่าคนยังไม่ล็อกอินอ่านไม่ได้ทุกที่ · เว็บยังเปิดได้ปกติหลังอัปกฎ

site เดิม `leaveeasy-korakot` ยังอยู่และยังเป็นไฟล์ของรอบก่อนหน้า ตั้งใจไม่แตะไว้อย่างนั้น
แต่ฐานข้อมูลกับกฎเป็นของ project เดียวกัน ทั้งสอง site จึงอยู่หลังกฎชุดใหม่นี้ด้วยกัน

**กติกาที่ยังต้องถือต่อไป**

รอบ deploy ครั้งถัดไปต้องสั่ง `firebase deploy --only hosting` และ
`firebase deploy --only firestore:rules` **ทั้งสองคำสั่ง**
การอัปหน้าเว็บอย่างเดียวแล้วลืมอัปกฎ คือการเอาเว็บใหม่ไปวางไว้หลังกฎเก่า
หลัง deploy ให้ยืนยันกับของจริงอีกครั้ง — งานที่ยังไม่ได้เห็นด้วยตาคืองานที่ยังไม่รู้ว่าผ่าน
