# ACL.md — ตารางสิทธิ์ของ LeaveEasy

ตารางนี้บอกว่าแต่ละบทบาท **ทำอะไรได้** และที่สำคัญกว่าคือ **ทำอะไรไม่ได้**

- บทบาทอ่านจากช่อง `role` ในไฟล์ `users/{uid}` ของคนที่ล็อกอินอยู่
- สมัครใหม่ทุกคนเป็น `employee` เสมอ · เปลี่ยนบทบาทได้ทางเดียวคือแก้ใน Firebase Console
- หน้าเว็บซ่อนปุ่มตามตารางนี้ แต่ **ประตูจริงคือ `firestore.rules`** — สองไฟล์นี้ต้องตรงกันทุกข้อ
- ที่มา: spec หัวข้อ 2 และ US-08

## ตารางสิทธิ์

| บทบาท | ทำได้ | ทำไม่ได้ |
|---|---|---|
| ผู้ขอลา `employee` | ยื่นใบลาในนามตัวเอง (สถานะเริ่มที่ `รอพิจารณา`) · ดูใบลาของตัวเอง · เขียนความเห็นในใบของตัวเอง · ลบใบของตัวเองที่ยัง `รอพิจารณา` · ใช้ปุ่ม AI จัดประเภทการลา · ดูรายชื่อประเภทการลา | **ดูใบลาของคนอื่น** (ทั้งในรายการ เปิดตรงด้วยลิงก์ และความเห็นในใบนั้น) · **อนุมัติหรือไม่อนุมัติใบลา รวมถึงใบของตัวเอง** · ยื่นใบลาในนามคนอื่น · ยื่นใบลาโดยตั้งสถานะเป็น `อนุมัติ` เอง · ลบใบที่พิจารณาแล้ว · เพิ่ม แก้ ลบ ประเภทการลา · เปลี่ยนบทบาทของตัวเอง · ใช้ AI สรุปใบลาหรือเปิดดู `aiLog` |
| ผู้อนุมัติ `manager` | ดูใบลาทุกใบ · อนุมัติหรือไม่อนุมัติใบที่ยัง `รอพิจารณา` (แก้ได้เฉพาะช่อง `status`) · เขียนความเห็นในทุกใบ · ใช้ AI สรุปใบลาและเปิดดู `aiLog` · ยื่นใบลาของตัวเอง | **อนุมัติใบลาของตัวเอง** · เปลี่ยนสถานะใบที่พิจารณาแล้ว · แก้ช่องอื่นของใบลาตอนอนุมัติ · ลบใบลาของคนอื่น · เพิ่ม แก้ ลบ ประเภทการลา · กำหนดผู้อนุมัติ · เปลี่ยนบทบาทของตัวเอง · แก้หรือลบความเห็นและ `aiLog` ที่บันทึกแล้ว |
| ฝ่ายบุคคล `hr` | ทุกอย่างที่ผู้อนุมัติทำได้ · เพิ่ม แก้ ลบ ประเภทการลา · กำหนดผู้อนุมัติให้แต่ละใบ (`approverId` · `approverName`) | **อนุมัติใบลาของตัวเอง** · ลบใบลาของคนอื่น · เปลี่ยนบทบาทของใครผ่านหน้าเว็บ (ทำได้ใน Console เท่านั้น) · แก้หรือลบความเห็นและ `aiLog` ที่บันทึกแล้ว |

## ผู้ช่วย AI กับสิทธิ์

- AI **เสนอ** เท่านั้น ไม่ได้ตัดสินแทน — สถานะเปลี่ยนได้ด้วยการกดของคนที่มีสิทธิ์เท่านั้น
- การเขียนสรุปลงช่อง `aiSuggestion` ต้องแก้ได้ช่องนี้ช่องเดียว กฎปฏิเสธถ้าแอบเปลี่ยน `status` มาพร้อมกัน
- `aiLog` เขียนต่อท้ายได้อย่างเดียว แก้หรือลบย้อนหลังไม่ได้

## วิธีทดสอบด้วยการสวมรอย

1. สมัคร 2 บัญชี (ชื่อสมมติ เช่น สมชาย กับ สมหญิง) · ทั้งคู่จะเป็น `employee`
2. ใช้บัญชีสมชายยื่นใบลา 1 ใบ แล้วคัดลอกลิงก์หน้ารายละเอียด
3. ออกจากระบบ → เข้าด้วยบัญชีสมหญิง → วางลิงก์ของสมชาย
   **ต้องเห็น:** "คุณไม่มีสิทธิ์เปิดใบลานี้" และหน้ารายการไม่มีใบของสมชาย
4. Firebase Console → `users/{uid ของสมหญิง}` → แก้ `role` เป็น `manager` → รีเฟรช
   **ต้องเห็น:** ใบของสมชายโผล่ และมีปุ่มอนุมัติ / ไม่อนุมัติ / ให้ AI ช่วยสรุปใบลา
5. สมหญิงยื่นใบลาของตัวเอง แล้วเปิดดู
   **ต้องเห็น:** ไม่มีปุ่มอนุมัติ และข้อความว่าอนุมัติใบของตัวเองไม่ได้
