# ACL — ใครทำอะไรได้บ้างใน LeaveEasy

เอกสารนี้อธิบาย `firestore.rules` เป็นภาษาไทยสำหรับคนอ่าน

> **ไฟล์ไหนคือความจริง**
> `firestore.rules` คือความจริงเสมอ ถ้าเอกสารนี้กับไฟล์กฎไม่ตรงกัน แปลว่าเอกสารนี้ผิด ให้แก้เอกสาร
> ไม่ใช่แก้กฎให้ตามเอกสาร

---

## 1. หลักการ 3 ข้อ

1. **ซ่อนปุ่มไม่ใช่ความปลอดภัย** — การซ่อนปุ่มบนหน้าเว็บคือการไม่ชวนให้กด ไม่ใช่การห้ามทำ
   ใครก็ตามที่เปิด console ของเบราว์เซอร์แล้วสั่ง Firestore ตรง ๆ ก็ข้ามหน้าเว็บของเราไปได้ทั้งหน้า
   `firestore.rules` ทำงานอยู่ฝั่ง Firebase จึงเป็นด่านเดียวที่ข้ามไม่ได้
2. **บทบาทอ่านจากฐานข้อมูล ไม่ใช่จากคำสั่งของผู้ใช้** — กฎอ่าน `role` จาก `users/{uid}` เสมอ
   ค่า `role` ที่ส่งแนบมากับคำสั่งเขียนจะไม่ถูกเชื่อถือ เพราะผู้ใช้แต่งค่านั้นเองได้
3. **ไม่มีกฎรองรับ = ปฏิเสธ** — โฟลเดอร์ที่ไม่ได้ระบุในตารางด้านล่างเข้าถึงไม่ได้เลย ทั้งอ่านและเขียน

## 2. บทบาทและสถานะ

| บทบาท (ค่าใน `users/{uid}.role`) | ใครเป็น |
|---|---|
| `employee` | พนักงานทั่วไป · ค่าเริ่มต้นของทุกคนที่สมัครเข้ามา |
| `manager` | หัวหน้าผู้พิจารณา · ตั้งได้จาก Firebase Console เท่านั้น |
| `hr` | ฝ่ายบุคคล · ตั้งได้จาก Firebase Console เท่านั้น |

สถานะใบลามี 3 ค่า: `รอพิจารณา` → `อนุมัติ` หรือ `ไม่อนุมัติ` (สองค่าหลังเป็นปลายทาง เปลี่ยนต่อไม่ได้)

## 3. ผู้ที่ยังไม่ได้เข้าสู่ระบบ

| ทำอะไรได้ |
|---|
| **ไม่ได้เลย** — อ่านไม่ได้ เขียนไม่ได้ ทุกโฟลเดอร์ ทุกไฟล์ |

ทุกกฎในไฟล์เริ่มต้นด้วยการเช็ค `request.auth != null` เสมอ

---

## 4. โฟลเดอร์ `users/{uid}`

ไฟล์โปรไฟล์คือที่เก็บ `role` ซึ่งเป็นหัวใจของกฎทุกข้อ
ถ้าใครเขียน `role` ของตัวเองได้ กฎที่เหลือทั้งไฟล์ก็ไม่มีความหมาย

| การกระทำ | employee | manager | hr | เงื่อนไข |
|---|---|---|---|---|
| อ่านไฟล์ของตัวเอง | ได้ | ได้ | ได้ | ชื่อไฟล์ต้องตรงกับ uid ของตัวเอง |
| อ่านไฟล์ของคนอื่น | **ไม่ได้** | ได้ | ได้ | manager/hr ต้องอ่านชื่อคนยื่นใบลามาแสดงในหน้ารายการ |
| ขอทั้งโฟลเดอร์ (list) | **ไม่ได้** | ได้ | ได้ | |
| สร้างไฟล์ | ได้ (เฉพาะของตัวเอง) | ได้ (เฉพาะของตัวเอง) | ได้ (เฉพาะของตัวเอง) | ชื่อไฟล์ = uid ตัวเอง **และ** `role == "employee"` **และ** มีได้แค่ 3 ช่อง `name`, `email`, `role` |
| แก้ไขไฟล์ | ได้ (เฉพาะช่อง `name` ของตัวเอง) | เหมือนกัน | เหมือนกัน | การเขียนต้องทำให้ค่าเปลี่ยนแค่ช่อง `name` เท่านั้น |
| แก้ `role` ของตัวเอง | **ไม่ได้** | **ไม่ได้** | **ไม่ได้** | เลื่อนตำแหน่งทำได้ทางเดียวคือแก้ใน Firebase Console |
| แก้ `email` | **ไม่ได้** | **ไม่ได้** | **ไม่ได้** | |
| แก้ไฟล์ของคนอื่น | **ไม่ได้** | **ไม่ได้** | **ไม่ได้** | hr ก็แก้ไม่ได้ ต้องเข้า Console |
| ลบไฟล์ | **ไม่ได้** | **ไม่ได้** | **ไม่ได้** | ใบลาเก่าอ้างถึง `requesterId` ของคนนั้นค้างอยู่ ลบแล้วประวัติจะชี้ไปหาคนที่ไม่มีตัวตน |

**ข้อที่กันคนตั้งตัวเองเป็นผู้บริหาร**
การสมัครสมาชิกต้องขอ `role` เป็น `employee` เท่านั้น ขอเป็น `hr` ตั้งแต่วินาทีแรกจะถูกปฏิเสธ
และเงื่อนไข "มีได้แค่ 3 ช่อง" กันช่องแปลกปลอมอย่าง `isAdmin` ที่เราไม่เคยออกแบบไว้ในสเปก

---

## 5. โฟลเดอร์ `leaveTypes/{id}`

ประเภทการลาเป็นข้อมูลกลางขององค์กร ไม่ใช่ข้อมูลส่วนตัวของใคร

| การกระทำ | employee | manager | hr |
|---|---|---|---|
| อ่าน (ทีละไฟล์และทั้งโฟลเดอร์) | ได้ | ได้ | ได้ |
| เพิ่ม | **ไม่ได้** | **ไม่ได้** | ได้ |
| แก้ชื่อ | **ไม่ได้** | **ไม่ได้** | ได้ |
| ลบ | **ไม่ได้** | **ไม่ได้** | ได้ |

ทุกคนที่ล็อกอินอ่านได้ เพราะหน้ายื่นใบลาต้องเอาไปใส่ในช่องเลือกประเภทการลา
หน้า `leave-types.html` เปิดดูได้ทุกบทบาท แต่ปุ่มเพิ่ม แก้ ลบ จะถูกฐานข้อมูลปฏิเสธถ้าไม่ใช่ hr

---

## 6. โฟลเดอร์ `leaveRequests/{id}`

| การกระทำ | employee | manager | hr | เงื่อนไข |
|---|---|---|---|---|
| อ่านใบของตัวเอง | ได้ | ได้ | ได้ | |
| อ่านใบของคนอื่น | **ไม่ได้** | ได้ | ได้ | นี่คือข้อที่การทดสอบสวมรอยใช้วัดผล |
| ขอทั้งโฟลเดอร์ (list) | **ไม่ได้** | ได้ | ได้ | ดูหัวข้อ 6.1 |
| ขอแบบมี `where("requesterId","==",uid)` | ได้ | ได้ | ได้ | |
| สร้างใบใหม่ | ได้ | ได้ | ได้ | `requesterId` ต้องเป็น uid ของตัวเอง **และ** `status` ต้องเป็น `รอพิจารณา` |
| เปลี่ยนสถานะใบของคนอื่น | **ไม่ได้** | ได้ | ได้ | ดูหัวข้อ 6.2 |
| เปลี่ยนสถานะ **ใบของตัวเอง** | **ไม่ได้** | **ไม่ได้** | **ไม่ได้** | แยกหน้าที่ผู้ขอกับผู้อนุมัติ · manager/hr ที่ยื่นใบลาเองต้องให้ผู้อนุมัติคนอื่นตัดสิน |
| แก้ช่องอื่น (หัวข้อ เหตุผล วันที่ ผู้อนุมัติ) | **ไม่ได้** | **ไม่ได้** | **ไม่ได้** | สเปกเดือนนี้ไม่มีฟังก์ชันแก้ใบลา |
| ลบใบ **ของตัวเอง** ที่ยัง `รอพิจารณา` | ได้ | ได้ | ได้ | กฎข้อนี้ดูที่ "เป็นเจ้าของใบไหม" อย่างเดียว **ไม่ได้ดูบทบาทเลย** · manager และ hr ที่ยื่นใบลาเอง ก็ถอนใบของตัวเองได้เหมือนพนักงานทั่วไป |
| ลบใบ **ของคนอื่น** | **ไม่ได้** | **ไม่ได้** | **ไม่ได้** | ไม่มีบทบาทไหนลบใบของคนอื่นได้ ถึงจะเป็น hr ก็ตาม |
| ลบใบที่ `อนุมัติ` หรือ `ไม่อนุมัติ` แล้ว | **ไม่ได้** | **ไม่ได้** | **ไม่ได้** | ใบที่ตัดสินไปแล้วคือประวัติขององค์กร ไม่ใช่ของส่วนตัวอีกต่อไป |

### 6.1 ทำไม employee ต้องใส่ `where` เสมอ

Firestore ตัดสิน **คำสั่งค้นหาทั้งคำสั่ง** ไม่ได้กรองผลทีละแถว
มันไม่ได้ไล่อ่านทุกใบแล้วคัดเฉพาะใบที่ผ่านกฎมาให้ แต่ดูที่เงื่อนไขของคำสั่งว่ารับประกันได้ไหม
ว่าทุกใบที่จะส่งกลับผ่านกฎแน่นอน ถ้ารับประกันไม่ได้ก็ปฏิเสธทั้งคำสั่งตั้งแต่ต้น

ผลที่ตามมา:

- `employee` ที่ขอทั้งโฟลเดอร์เปล่า ๆ จะได้ **permission error** ไม่ใช่ได้รายการสั้น ๆ ของตัวเอง
- `employee` ต้องยิง `where("requesterId","==",uid)` ติดไปด้วยเสมอ
- `manager` และ `hr` ขอทั้งโฟลเดอร์ได้ เพราะเงื่อนไข "เป็นผู้อนุมัติ" ไม่ได้ขึ้นกับตัวเอกสารแต่ละใบ

หน้าจอที่ต้องระวังข้อนี้: `leave-requests.js` และ `dashboard.js`

### 6.2 การเปลี่ยนสถานะ · เงื่อนไข 4 ชั้น

ขาดชั้นใดชั้นหนึ่งไม่ได้

1. **คนเปลี่ยนต้องเป็น `manager` หรือ `hr`** — พนักงานทั่วไปกดอนุมัติไม่ได้
2. **ห้ามอนุมัติหรือไม่อนุมัติใบของตัวเอง** (`resource.data.requesterId != request.auth.uid`)
   นี่คือหลัก **แยกหน้าที่ผู้ขอกับผู้อนุมัติ**
   หัวหน้าและฝ่ายบุคคลก็ลาได้ และเมื่อลาก็เป็น "ผู้ขอ" เหมือนพนักงานคนอื่น
   ใบของตัวเองต้องให้ผู้อนุมัติคนอื่นเป็นคนตัดสิน ไม่ใช่เซ็นอนุมัติให้ตัวเอง
3. **ใบนั้นต้องยังอยู่ที่ `รอพิจารณา`** และเปลี่ยนไปได้แค่ `อนุมัติ` หรือ `ไม่อนุมัติ`
   ใบที่ตัดสินไปแล้วแก้ซ้ำไม่ได้ ย้อนกลับมาเป็น `รอพิจารณา` ก็ไม่ได้
4. **การเขียนต้องทำให้ค่าเปลี่ยนแค่ช่อง `status` ช่องเดียว**
   ชั้นนี้กันการแอบแก้ `reason` หรือ `startDate` แนบไปกับการกดอนุมัติ
   ฝั่งโค้ดต้องใช้ `updateDoc({ status })` ห้ามใช้ `setDoc` ที่เขียนทับทั้งไฟล์

> ชั้นที่ 2 ทำให้ใบลาของ `manager` หรือ `hr` **ค้างที่ `รอพิจารณา` ได้ถาวร**
> ถ้าในองค์กรมีผู้อนุมัติอยู่คนเดียว นี่เป็นผลที่ตั้งใจ ไม่ใช่บั๊ก
> ทางออกคือมีผู้อนุมัติอย่างน้อย 2 คน ซึ่งตรงกับสเปกที่ระบุว่ามีหัวหน้าผู้อนุมัติ 1–3 คน

---

## 7. โฟลเดอร์ย่อย `leaveRequests/{id}/approvals/{id}`

สิทธิ์ของโฟลเดอร์แม่ **ไม่ได้ไหลลงมาให้อัตโนมัติ** กฎที่นี่เขียนแยกต่างหาก

| การกระทำ | เจ้าของใบลาแม่ | manager | hr | employee คนอื่น |
|---|---|---|---|---|
| อ่านความเห็น | ได้ | ได้ | ได้ | **ไม่ได้** |
| เขียนความเห็นใหม่ | ได้ | ได้ | ได้ | **ไม่ได้** (กฎเช็คความเกี่ยวข้องกับใบแม่ตรง ๆ) |
| แก้ความเห็นที่เขียนไปแล้ว | **ไม่ได้** | **ไม่ได้** | **ไม่ได้** | **ไม่ได้** |
| ลบความเห็น | **ไม่ได้** | **ไม่ได้** | **ไม่ได้** | **ไม่ได้** |

การเขียนความเห็นต้องผ่าน **2 ด่านพร้อมกัน**

1. **ลงชื่อตัวเอง** — `authorId` ต้องตรงกับ uid ของคนที่ล็อกอินอยู่ ลงชื่อเป็นคนอื่นไม่ได้
2. **ต้องเกี่ยวข้องกับใบลาใบนั้นจริง** — เป็นเจ้าของใบ หรือเป็น `manager` / `hr`

> **ทำไมต้องมีด่านที่ 2 ทั้งที่ด่านแรกกันการปลอมชื่อไปแล้ว**
> เพราะ **การเขียนไม่ต้องอ่านก่อน** ถ้ามีแต่ด่านแรก พนักงานคนใดก็ได้ที่ล็อกอินแล้ว
> และรู้รหัสใบลา จะเขียนความเห็นแปะลงใบลาของคนแปลกหน้าได้ เขาอ่านใบนั้นกลับไม่ได้ก็จริง
> แต่ข้อความไปโผล่ต่อหน้าผู้อนุมัติแล้ว เท่ากับยิงข้อความใส่เอกสารที่ตัวเองไม่มีส่วนเกี่ยวข้อง
> สเปก US-08 บอกว่าพนักงานคนหนึ่งต้องไม่เอื้อมถึงใบลาของอีกคน — **การเขียนก็คือการเอื้อมถึงแบบหนึ่ง ไม่ใช่แค่การอ่าน**

- ตั้งใจให้เจ้าของใบเขียนความเห็นในใบตัวเองได้ด้วย เพราะต้องตอบคำถามของผู้อนุมัติ
- ความเห็นแก้ไม่ได้ลบไม่ได้ เพราะเป็นบันทึกการพิจารณา ถ้าย้อนกลับไปแก้ได้ก็ใช้อ้างอิงไม่ได้อีก

---

## 8. โฟลเดอร์อื่นทั้งหมด

| การกระทำ | ทุกบทบาท |
|---|---|
| อ่าน | **ไม่ได้** |
| เขียน | **ไม่ได้** |

Firestore ปฏิเสธสิ่งที่ไม่มีกฎรองรับอยู่แล้วโดยปริยาย
ในไฟล์กฎมีข้อ `match /{document=**} { allow read, write: if false; }` เขียนไว้ท้ายสุด
เพื่อให้คนอ่านเห็นชัดว่าเป็นความตั้งใจ ไม่ใช่ลืมเขียน และข้อนี้ไม่ได้เปิดสิทธิ์เพิ่มให้ใคร

---

## 9. สิ่งที่ `firestore.rules` บังคับไม่ได้

บันทึกไว้ให้ชัดว่ารู้อยู่ ไม่ใช่มองข้าม

> ข้อแรกในตารางนี้คือข้อจำกัดของตัวภาษา ไม่ใช่สิ่งที่เขียนกฎเก่งกว่านี้แล้วจะแก้ได้
> **ห้ามหาทางอ้อมให้บังคับได้ และห้ามผ่อนกฎข้ออื่นเพื่อแลกกับเรื่องนี้**
> เดือนนี้กฎข้อนั้นบังคับในเบราว์เซอร์อย่างเดียว และยอมรับข้อจำกัดนี้ไว้ตามที่บันทึก

| กฎในสเปก | ทำไมกฎ Firestore บังคับไม่ได้ | ใครบังคับแทน |
|---|---|---|
| ต้องมีความเห็นอย่างน้อย 1 รายการก่อนตั้ง `ไม่อนุมัติ` (สเปกหัวข้อ 6) | กฎ Firestore เปิดดูได้แค่ไฟล์ที่รู้ชื่อเป๊ะ ๆ ผ่าน `exists()` / `get()` แต่ **สั่งค้นหาทั้งโฟลเดอร์ไม่ได้** จึงนับจำนวนความเห็นในโฟลเดอร์ย่อยไม่ได้เลย | `leave-request-detail.js` ตรวจก่อนเรียก `updateLeaveStatus` |
| กันใบลาวันเริ่มหลังวันสิ้นสุด | กฎเทียบข้อความวันที่ได้ แต่สเปกหัวข้อ 9 ไม่ได้สั่งไว้ จึงไม่ใส่เพื่อไม่ให้ต่างจากรายการที่ตกลงกัน | `new-leave-request.js` ตรวจในฟอร์ม |
| ตั้งใครเป็น `manager` / `hr` | เป็นงานของผู้ดูแลระบบ ไม่ใช่ของผู้ใช้ | Firebase Console |

---

## 10. การใส่ข้อมูลตัวอย่าง (seed) ทำงานยังไงภายใต้กฎชุดนี้

`js/seed.js` เขียนไฟล์ด้วย **รหัสคงที่** (`u001`–`u003`, `lr001`–`lr005`, `ap001`–`ap004`)
และเขียนใบลาที่มีสถานะ `อนุมัติ` / `ไม่อนุมัติ` มาตั้งแต่ต้น

กฎชุดนี้ **ปฏิเสธการเขียนเหล่านั้นทั้งหมด** และนั่นถูกต้องแล้ว ไม่ใช่บั๊ก
เพราะถ้ากฎยอมให้ใครสักคนเขียน `users/u002` ที่มี `role: "manager"` ได้ ก็แปลว่ากฎข้อ
"สมัครมาได้แค่ employee" ไร้ผลทันที

**ทางที่ถูกต้องของการ seed คือใช้ช่องทางของผู้ดูแลระบบ ซึ่งข้าม `firestore.rules` โดยการออกแบบ**

| สภาพแวดล้อม | วิธี seed ที่ใช้จริง |
|---|---|
| การทดสอบอัตโนมัติ (emulator) | `tests/fixtures/emulator.js` ยิง REST ของ emulator ด้วย header `Authorization: Bearer owner` ซึ่งเป็นสิทธิ์ผู้ดูแล ข้ามกฎได้ · เป็นช่องทางที่วางไว้แล้วและใช้อยู่ |
| ดูข้อมูลด้วยตาระหว่างพัฒนา (emulator) | Emulator UI ที่ `http://localhost:4000` เขียนในฐานะผู้ดูแลเช่นกัน |
| ฐานข้อมูลจริงบน Firebase | Firebase Console เขียนในฐานะเจ้าของโปรเจกต์ ข้ามกฎได้ · เป็นที่เดียวกับที่ใช้ตั้ง `role` ให้ manager / hr อยู่แล้ว |

**ห้ามแก้ `firestore.rules` ให้หย่อนลงเพื่อให้ `seed.html` กดผ่าน** เพราะรูที่เจาะเพื่อ seed
คือรูเดียวกับที่คนนอกใช้ตั้งตัวเองเป็น hr

`seed.html` จึงเป็นเครื่องมือสำหรับฐานจำลองหรือสำหรับเดโม ไม่ใช่ทางที่ข้อมูลตัวอย่างเข้าระบบจริง
รายละเอียดว่าแต่ละบรรทัดถูกปฏิเสธด้วยกฎข้อไหน ดูในรายงานของงานกฎความปลอดภัย

---

## 11. วิธีตรวจว่ากฎยังทำงานถูกต้อง

การทดสอบที่ใช้วัดผลจริงคือ **การสวมรอย** — ล็อกอินเป็นพนักงานคนหนึ่ง แล้วต้องอ่านใบลาของ
พนักงานอีกคนไม่ได้ ถ้าอ่านได้แปลว่ากฎพัง ไม่ว่าหน้าจอจะซ่อนปุ่มไว้เรียบร้อยแค่ไหนก็ตาม

รายการที่ควรตรวจอย่างน้อย

- [ ] ไม่ล็อกอิน → อ่าน `leaveRequests` ไม่ได้
- [ ] employee A → อ่านใบของ employee B ไม่ได้
- [ ] employee → ขอทั้งโฟลเดอร์ `leaveRequests` ไม่ได้ แต่ขอแบบมี `where` ได้
- [ ] employee → สมัครโดยขอ `role: "hr"` ไม่ได้
- [ ] employee → แก้ `role` ของตัวเองเป็น `hr` ไม่ได้
- [ ] employee → อนุมัติใบของตัวเองไม่ได้
- [ ] manager → อนุมัติหรือไม่อนุมัติ **ใบที่ตัวเองยื่น** ไม่ได้ (แยกหน้าที่)
- [ ] hr → อนุมัติหรือไม่อนุมัติ **ใบที่ตัวเองยื่น** ไม่ได้ (แยกหน้าที่)
- [ ] manager → อนุมัติใบของคนอื่นได้ตามปกติ
- [ ] manager → กดอนุมัติแล้วแอบแก้ `reason` ไปด้วยไม่ได้
- [ ] manager → เปลี่ยนสถานะใบที่ `อนุมัติ` ไปแล้วซ้ำไม่ได้
- [ ] manager → ลบใบลาของคนอื่นไม่ได้ (แต่ลบใบของตัวเองที่ยัง `รอพิจารณา` ได้)
- [ ] employee ที่ไม่เกี่ยวข้อง → เขียนความเห็นลงใบของคนอื่นไม่ได้ แม้จะรู้รหัสใบลาก็ตาม
- [ ] manager → เพิ่มประเภทการลาไม่ได้ (ต้องเป็น hr)
- [ ] ความเห็นที่เขียนไปแล้ว แก้ไม่ได้และลบไม่ได้
- [ ] โฟลเดอร์ที่ไม่มีในเอกสารนี้ อ่านและเขียนไม่ได้
